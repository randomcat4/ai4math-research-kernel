# Matlas thin-client attribution

The request protocol in `matlas.py` is adapted from
`FrenzyMath/Danus/danus/integrations/matlas.py`, licensed Apache-2.0.

RK reuses only the unauthenticated HTTP thin-client contract for
`https://leansearch.net/thm/search`. This package does not contain or claim the Matlas
service, corpus, theorem dependency graph, embedding model, or vector index.
